#class_name CharacterManager
extends Node


func update(event: StoryEvent):
	#var summirized_events = Summarizer.summarize_events(event)
	#for character in event.characters:
		#character.memory.append(summirized_events)
	#
#
	#for relationship in relationships:
		#relationship.update()
		pass
